# 4. Node-by-node reference

> Generated by `node scripts/docs-workflows.mjs` from `n8n/workflows/*.json`.
> Do not edit by hand - change `n8n/src/*.mjs`, run `npm run build:n8n`, then regenerate.

Every workflow here is importable as-is. Two credentials must exist in n8n before
they run: an HTTP Header Auth credential named **Supabase Service Role** and one
named **Anthropic API Key**. Everything else comes from environment variables -
see `.env.example` and `docs/05-integrations.md`.

---

## wf01-lead-discovery

Discover businesses for a campaign and land them at stage SCRAPED.

**17 nodes.** Import from `n8n/workflows/wf01-lead-discovery.json`.

| Node | Type | Talks to | Reliability | Why it is there |
| --- | --- | --- | --- | --- |
| Daily Discovery | Schedule Trigger | cron `0 6 * * 1-5` | - |  |
| Called By Orchestrator | Execute Workflow Trigger |  | - | Input: { campaign_id, batch_size?, execution_id? } |
| Load Campaign + Niche | HTTP Request | db: `GET campaigns` | retry x3 | One read per run. Campaign settings - ICP band, thresholds, daily targets, follow-up offsets - all come from here, never from node parameters. |
| Start Run Record | HTTP Request | db: `POST workflow_runs` | retry x3 | Resume bookkeeping: which workflow, which campaign, which execution, and how far it got. |
| Build Search Terms | Code | inlines `lib/normalize.js`, `lib/search-terms.js` | - | Each output item is one search term. The per-term limit is derived from the campaign daily target so a wide city list does not blow the budget. |
| For Each Search Term | Loop Over Items |  | - |  |
| Run Scraper (Apify) | HTTP Request | Apify | retry x3, error output | Replaceable. Any endpoint that returns rows lib/normalize.js can read works here - see config/providers.json capability "discovery" and lib/providers/discovery.*.js. |
| Normalise + Dedupe Batch | Code | inlines `lib/normalize.js`, `lib/dedupe.js` | - | In-memory deduplication. The database is the second wall - resolve_company() matches on the same identity keys. |
| Resolve Company (dedupe) | HTTP Request | db: `resolve_company()` | retry x3 | Returns the existing company when any identity key matches, otherwise creates one. Every key seen is recorded, so a later hit on ANY of them lands on the same row. |
| Suppressed? | HTTP Request | db: `is_suppressed()` | retry x3 | Opt-outs and do-not-contact are checked before a lead is ever created. |
| Not Suppressed | IF |  | - | Suppressed businesses never become leads. An ambiguous response is treated as not suppressed, and the unique constraints downstream still prevent duplicates. |
| Create Lead (SCRAPED) | HTTP Request | db: `upsert_lead()` | retry x3 | unique(campaign_id, company_id) guarantees a business is never queued twice inside one campaign. |
| Skip Suppressed | No Op |  | - |  |
| Summarise Batch | Code | inline logic | - |  |
| Finish Run Record | HTTP Request | db: `PATCH workflow_runs` | retry x3, continues on fail |  |
| Classify Failure | Code | inlines `lib/retry.js` | - | Uses the same classifier as the test suite: 429 backs off, 5xx retries, 401/422 dead-letters immediately. |
| Log Discovery Error | HTTP Request | db: `POST errors` | retry x3, continues on fail |  |

**Connections**

```
Daily Discovery -> Load Campaign + Niche
Called By Orchestrator -> Load Campaign + Niche
Load Campaign + Niche -> Start Run Record
Start Run Record -> Build Search Terms
Build Search Terms -> For Each Search Term
For Each Search Term (output 0) -> Summarise Batch
For Each Search Term (output 1) -> Run Scraper (Apify)
Run Scraper (Apify) (output 0) -> Normalise + Dedupe Batch
Run Scraper (Apify) (output 1, error) -> Classify Failure
Normalise + Dedupe Batch -> Resolve Company (dedupe)
Resolve Company (dedupe) -> Suppressed?
Suppressed? -> Not Suppressed
Not Suppressed (output 0) -> Create Lead (SCRAPED)
Not Suppressed (output 1) -> Skip Suppressed
Create Lead (SCRAPED) -> For Each Search Term
Skip Suppressed -> For Each Search Term
Summarise Batch -> Finish Run Record
Classify Failure -> Log Discovery Error
Log Discovery Error -> For Each Search Term
```

## wf02-clean-dedupe

Normalise, filter and de-duplicate scraped leads.

**14 nodes.** Import from `n8n/workflows/wf02-clean-dedupe.json`.

| Node | Type | Talks to | Reliability | Why it is there |
| --- | --- | --- | --- | --- |
| Every 15 Minutes | Schedule Trigger | cron `*/15 6-20 * * 1-5` | - |  |
| Called By Orchestrator | Execute Workflow Trigger |  | - | Input: { campaign_id, batch_size?, execution_id? } |
| Load Campaign + Niche | HTTP Request | db: `GET campaigns` | retry x3 | One read per run. Campaign settings - ICP band, thresholds, daily targets, follow-up offsets - all come from here, never from node parameters. |
| Claim SCRAPED Leads | HTTP Request | db: `claim_leads()` | retry x3 | SELECT ... FOR UPDATE SKIP LOCKED, so parallel executions never hand the same lead to two workers. A claim older than 20 minutes is treated as abandoned and reissued, which is how a crashed run resumes instead of stranding leads at SCRAPED. |
| Fetch Company Record | HTTP Request | db: `GET companies` | retry x3 | One read per claimed lead. Swap for a single ?id=in.(...) read if batch sizes grow beyond a few hundred. |
| Apply Cleaning Rules | Code | inlines `lib/normalize.js`, `lib/clean.js`, `lib/dedupe.js` | - | Rules come from config/cleaning.json, inlined at build time. Edit that file and rebuild to change what is filtered - no node edits. |
| Keep or Drop | Switch |  | - |  |
| Tag Company With Niche | HTTP Request | db: `PATCH companies` | retry x3, continues on fail | Only the niche tagging. The cleaning flags travel on the advance_lead detail into the activity log - writing them here would overwrite companies.raw, which holds the provider payload. |
| Store Cleaning Flags | HTTP Request | db: `PATCH leads` | retry x3 | Niche keyword hits, personal-account signals and location reasoning. Qualification scores niche fit from these, so they must survive the cleaning run. |
| Advance To CLEANED | HTTP Request | db: `advance_lead()` | retry x3 |  |
| Record Drop Reason | HTTP Request | db: `PATCH leads` | retry x3 |  |
| Advance To DISQUALIFIED | HTTP Request | db: `advance_lead()` | retry x3 |  |
| Classify Failure | Code | inlines `lib/retry.js` | - | Uses the same classifier as the test suite: 429 backs off, 5xx retries, 401/422 dead-letters immediately. |
| Record Failure | HTTP Request | db: `fail_lead()` | retry x3, continues on fail | The failed-item queue. The lead returns to its stage with an incremented retry count and an exponential backoff; the rest of the batch is unaffected. |

**Connections**

```
Every 15 Minutes -> Load Campaign + Niche
Called By Orchestrator -> Load Campaign + Niche
Load Campaign + Niche -> Claim SCRAPED Leads
Claim SCRAPED Leads -> Fetch Company Record
Fetch Company Record (output 0) -> Apply Cleaning Rules
Fetch Company Record (output 1) -> Classify Failure
Apply Cleaning Rules -> Keep or Drop
Keep or Drop (output 0) -> Tag Company With Niche
Keep or Drop (output 1) -> Record Drop Reason
Tag Company With Niche -> Store Cleaning Flags
Store Cleaning Flags -> Advance To CLEANED
Record Drop Reason -> Advance To DISQUALIFIED
Classify Failure -> Record Failure
```

## wf03-enrichment

Enrich cleaned leads with company and contact data, then choose the decision maker.

**15 nodes.** Import from `n8n/workflows/wf03-enrichment.json`.

| Node | Type | Talks to | Reliability | Why it is there |
| --- | --- | --- | --- | --- |
| Every 15 Minutes | Schedule Trigger | cron `*/15 6-20 * * 1-5` | - |  |
| Called By Orchestrator | Execute Workflow Trigger |  | - | Input: { campaign_id, batch_size?, execution_id? } |
| Load Campaign + Niche | HTTP Request | db: `GET campaigns` | retry x3 | One read per run. Campaign settings - ICP band, thresholds, daily targets, follow-up offsets - all come from here, never from node parameters. |
| Claim CLEANED Leads | HTTP Request | db: `claim_leads()` | retry x3 | SELECT ... FOR UPDATE SKIP LOCKED, so parallel executions never hand the same lead to two workers. A claim older than 20 minutes is treated as abandoned and reissued, which is how a crashed run resumes instead of stranding leads at CLEANED. |
| Fetch Company Record | HTTP Request | db: `GET companies` | retry x3 |  |
| Prepare Enrichment Input | Code | inline logic | - |  |
| Domain Available? | IF |  | - | Apollo searches on a domain. Without one, go straight to the fallback rather than burning a credit. |
| Enrich (Apollo) | HTTP Request | Apollo | retry x3, error output | Replaceable: config/providers.json capability "enrichment". The fallback chain is declared there, not hard-coded here. |
| Enrich (Website Fallback) | HTTP Request | HTTP | retry x3, error output | Runs when the paid provider finds nobody. This is what keeps founder-led businesses - the best prospects - in the pipeline. |
| Merge Contacts + Pick Decision Maker | Code | inlines `lib/decision-maker.js`, `lib/normalize.js` | - | Ranks contacts by company size: founder/owner for small businesses, the person who owns the support queue for larger ones. |
| Store Contacts | HTTP Request | db: `POST contacts` | retry x3, continues on fail | Upsert on (company_id, email). Contacts without an email are inserted once per run and de-duplicated by the partial unique index. |
| Link Decision Maker To Lead | HTTP Request | db: `PATCH leads` | retry x3, continues on fail |  |
| Advance To ENRICHED | HTTP Request | db: `advance_lead()` | retry x3 |  |
| Classify Failure | Code | inlines `lib/retry.js` | - | Uses the same classifier as the test suite: 429 backs off, 5xx retries, 401/422 dead-letters immediately. |
| Record Failure | HTTP Request | db: `fail_lead()` | retry x3, continues on fail | The failed-item queue. The lead returns to its stage with an incremented retry count and an exponential backoff; the rest of the batch is unaffected. |

**Connections**

```
Every 15 Minutes -> Load Campaign + Niche
Called By Orchestrator -> Load Campaign + Niche
Load Campaign + Niche -> Claim CLEANED Leads
Claim CLEANED Leads -> Fetch Company Record
Fetch Company Record (output 0) -> Prepare Enrichment Input
Fetch Company Record (output 1) -> Classify Failure
Prepare Enrichment Input -> Domain Available?
Domain Available? (output 0) -> Enrich (Apollo)
Domain Available? (output 1) -> Enrich (Website Fallback)
Enrich (Apollo) (output 0) -> Merge Contacts + Pick Decision Maker
Enrich (Apollo) (output 1, error) -> Enrich (Website Fallback)
Enrich (Website Fallback) -> Merge Contacts + Pick Decision Maker
Merge Contacts + Pick Decision Maker -> Store Contacts
Store Contacts -> Link Decision Maker To Lead
Link Decision Maker To Lead -> Advance To ENRICHED
Classify Failure -> Record Failure
```

## wf04-qualification

Qualify enriched leads with Claude and compute the ICP score.

**17 nodes.** Import from `n8n/workflows/wf04-qualification.json`.

| Node | Type | Talks to | Reliability | Why it is there |
| --- | --- | --- | --- | --- |
| Every 15 Minutes | Schedule Trigger | cron `*/15 6-20 * * 1-5` | - |  |
| Called By Orchestrator | Execute Workflow Trigger |  | - | Input: { campaign_id, batch_size?, execution_id? } |
| Load Campaign + Niche | HTTP Request | db: `GET campaigns` | retry x3 | One read per run. Campaign settings - ICP band, thresholds, daily targets, follow-up offsets - all come from here, never from node parameters. |
| Claim ENRICHED Leads | HTTP Request | db: `claim_leads()` | retry x3 | SELECT ... FOR UPDATE SKIP LOCKED, so parallel executions never hand the same lead to two workers. A claim older than 20 minutes is treated as abandoned and reissued, which is how a crashed run resumes instead of stranding leads at ENRICHED. |
| Fetch Lead Detail | HTTP Request | db: `GET v_leads_full` | retry x3 | The flat view: company facts, the chosen decision maker and the campaign in one row. |
| Render Qualification Prompt | Code | inlines `lib/prompts.js`, `lib/normalize.js` | - | The prompt file prompts/01-qualification.md is inlined at build time. Rendering throws if a required variable is missing, so a half-filled prompt is never sent. |
| Claude: Qualify | HTTP Request | Claude (Messages API) | retry x3, error output | Node-level retries cover 429 and 5xx. A malformed or schema-invalid response is handled by the parser node, which sends one correction turn before failing the item. |
| Parse + Score | Code | inlines `lib/validate.js`, `lib/json.js`, `lib/scoring.js` | - | The model never returns the final score. computeIcpScore() blends its judgement with measured facts, so the same lead always scores the same and explainScore() can justify every point. |
| Valid Response? | Switch |  | - |  |
| Claude: Repair JSON | HTTP Request | Claude (Messages API) | retry x3, error output | Node-level retries cover 429 and 5xx. A malformed or schema-invalid response is handled by the parser node, which sends one correction turn before failing the item. |
| Store Qualification Result | HTTP Request | db: `POST qualification_results` | retry x3 | Every run is kept. Re-qualifying a lead after a config change leaves the previous verdict in place for comparison. |
| Update Lead Score | HTTP Request | db: `PATCH leads` | retry x3 |  |
| Qualified? | Switch |  | - |  |
| Advance To QUALIFIED | HTTP Request | db: `advance_lead()` | retry x3 |  |
| Advance To DISQUALIFIED | HTTP Request | db: `advance_lead()` | retry x3 |  |
| Classify Failure | Code | inlines `lib/retry.js` | - | Uses the same classifier as the test suite: 429 backs off, 5xx retries, 401/422 dead-letters immediately. |
| Record Failure | HTTP Request | db: `fail_lead()` | retry x3, continues on fail | The failed-item queue. The lead returns to its stage with an incremented retry count and an exponential backoff; the rest of the batch is unaffected. |

**Connections**

```
Every 15 Minutes -> Load Campaign + Niche
Called By Orchestrator -> Load Campaign + Niche
Load Campaign + Niche -> Claim ENRICHED Leads
Claim ENRICHED Leads -> Fetch Lead Detail
Fetch Lead Detail (output 0) -> Render Qualification Prompt
Fetch Lead Detail (output 1) -> Classify Failure
Render Qualification Prompt -> Claude: Qualify
Claude: Qualify (output 0) -> Parse + Score
Claude: Qualify (output 1, error) -> Classify Failure
Parse + Score -> Valid Response?
Valid Response? (output 0) -> Store Qualification Result
Valid Response? (output 1) -> Claude: Repair JSON
Claude: Repair JSON -> Parse + Score
Store Qualification Result -> Update Lead Score
Update Lead Score -> Qualified?
Qualified? (output 0) -> Advance To QUALIFIED
Qualified? (output 1) -> Advance To DISQUALIFIED
Classify Failure -> Record Failure
```

## wf05-research

Write a grounded research brief for each qualified lead.

**17 nodes.** Import from `n8n/workflows/wf05-research.json`.

| Node | Type | Talks to | Reliability | Why it is there |
| --- | --- | --- | --- | --- |
| Every 20 Minutes | Schedule Trigger | cron `*/20 6-20 * * 1-5` | - |  |
| Called By Orchestrator | Execute Workflow Trigger |  | - | Input: { campaign_id, batch_size?, execution_id? } |
| Load Campaign + Niche | HTTP Request | db: `GET campaigns` | retry x3 | One read per run. Campaign settings - ICP band, thresholds, daily targets, follow-up offsets - all come from here, never from node parameters. |
| Claim QUALIFIED Leads | HTTP Request | db: `claim_leads()` | retry x3 | SELECT ... FOR UPDATE SKIP LOCKED, so parallel executions never hand the same lead to two workers. A claim older than 20 minutes is treated as abandoned and reissued, which is how a crashed run resumes instead of stranding leads at QUALIFIED. |
| In A Researched Band? | IF |  | - | REVIEW-band leads are not researched automatically - a human decides whether they are worth the tokens. |
| Fetch Lead Detail | HTTP Request | db: `GET v_leads_full` | retry x3 |  |
| Hold For Manual Review | HTTP Request | db: `PATCH leads` | retry x3, continues on fail |  |
| Render Research Prompt | Code | inlines `lib/prompts.js`, `lib/normalize.js` | - |  |
| Claude: Research | HTTP Request | Claude (Messages API) | retry x3, error output | Node-level retries cover 429 and 5xx. A malformed or schema-invalid response is handled by the parser node, which sends one correction turn before failing the item. |
| Parse Research | Code | inlines `lib/validate.js`, `lib/json.js`, `lib/angles.js` | - |  |
| Valid Research? | Switch |  | - |  |
| Claude: Repair Research JSON | HTTP Request | Claude (Messages API) | retry x3, error output | Node-level retries cover 429 and 5xx. A malformed or schema-invalid response is handled by the parser node, which sends one correction turn before failing the item. |
| Store Research Brief | HTTP Request | db: `POST research` | retry x3 |  |
| Update Lead Angle | HTTP Request | db: `PATCH leads` | retry x3 |  |
| Advance To RESEARCHED | HTTP Request | db: `advance_lead()` | retry x3 |  |
| Classify Failure | Code | inlines `lib/retry.js` | - | Uses the same classifier as the test suite: 429 backs off, 5xx retries, 401/422 dead-letters immediately. |
| Record Failure | HTTP Request | db: `fail_lead()` | retry x3, continues on fail | The failed-item queue. The lead returns to its stage with an incremented retry count and an exponential backoff; the rest of the batch is unaffected. |

**Connections**

```
Every 20 Minutes -> Load Campaign + Niche
Called By Orchestrator -> Load Campaign + Niche
Load Campaign + Niche -> Claim QUALIFIED Leads
Claim QUALIFIED Leads -> In A Researched Band?
In A Researched Band? (output 0) -> Fetch Lead Detail
In A Researched Band? (output 1) -> Hold For Manual Review
Fetch Lead Detail -> Render Research Prompt
Render Research Prompt -> Claude: Research
Claude: Research (output 0) -> Parse Research
Claude: Research (output 1, error) -> Classify Failure
Parse Research -> Valid Research?
Valid Research? (output 0) -> Store Research Brief
Valid Research? (output 1) -> Claude: Repair Research JSON
Claude: Repair Research JSON -> Parse Research
Store Research Brief -> Update Lead Angle
Update Lead Angle -> Advance To RESEARCHED
Classify Failure -> Record Failure
```

## wf06-outreach-generation

Generate personalised Instagram DM variations for researched leads.

**15 nodes.** Import from `n8n/workflows/wf06-outreach-generation.json`.

| Node | Type | Talks to | Reliability | Why it is there |
| --- | --- | --- | --- | --- |
| Every 20 Minutes | Schedule Trigger | cron `*/20 6-20 * * 1-5` | - |  |
| Called By Orchestrator | Execute Workflow Trigger |  | - | Input: { campaign_id, batch_size?, execution_id? } |
| Load Campaign + Niche | HTTP Request | db: `GET campaigns` | retry x3 | One read per run. Campaign settings - ICP band, thresholds, daily targets, follow-up offsets - all come from here, never from node parameters. |
| Claim RESEARCHED Leads | HTTP Request | db: `claim_leads()` | retry x3 | SELECT ... FOR UPDATE SKIP LOCKED, so parallel executions never hand the same lead to two workers. A claim older than 20 minutes is treated as abandoned and reissued, which is how a crashed run resumes instead of stranding leads at RESEARCHED. |
| Fetch Lead + Research | HTTP Request | db: `GET v_leads_full` | retry x3 |  |
| Fetch Research Brief | HTTP Request | db: `GET research` | retry x3 |  |
| Render Outreach Prompt | Code | inlines `lib/prompts.js`, `lib/angles.js` | - |  |
| Claude: Write DM Variations | HTTP Request | Claude (Messages API) | retry x3, error output | Node-level retries cover 429 and 5xx. A malformed or schema-invalid response is handled by the parser node, which sends one correction turn before failing the item. |
| Parse + Guardrails | Code | inlines `lib/validate.js`, `lib/json.js` | - | Character limits, banned phrases, emoji count and question count are enforced in code. A flagged draft still reaches the reviewer - it is never sent automatically and never silently discarded. |
| Valid Message? | Switch |  | - |  |
| Claude: Repair Message JSON | HTTP Request | Claude (Messages API) | retry x3, error output | Node-level retries cover 429 and 5xx. A malformed or schema-invalid response is handled by the parser node, which sends one correction turn before failing the item. |
| Queue Draft For Approval | HTTP Request | db: `POST outreach` | retry x3 | Status PENDING_REVIEW. There is no path from this workflow to a sent message - approval is a separate, human step. |
| Advance To PENDING_APPROVAL | HTTP Request | db: `advance_lead()` | retry x3 |  |
| Classify Failure | Code | inlines `lib/retry.js` | - | Uses the same classifier as the test suite: 429 backs off, 5xx retries, 401/422 dead-letters immediately. |
| Record Failure | HTTP Request | db: `fail_lead()` | retry x3, continues on fail | The failed-item queue. The lead returns to its stage with an incremented retry count and an exponential backoff; the rest of the batch is unaffected. |

**Connections**

```
Every 20 Minutes -> Load Campaign + Niche
Called By Orchestrator -> Load Campaign + Niche
Load Campaign + Niche -> Claim RESEARCHED Leads
Claim RESEARCHED Leads -> Fetch Lead + Research
Fetch Lead + Research -> Fetch Research Brief
Fetch Research Brief -> Render Outreach Prompt
Render Outreach Prompt -> Claude: Write DM Variations
Claude: Write DM Variations (output 0) -> Parse + Guardrails
Claude: Write DM Variations (output 1, error) -> Classify Failure
Parse + Guardrails -> Valid Message?
Valid Message? (output 0) -> Queue Draft For Approval
Valid Message? (output 1) -> Claude: Repair Message JSON
Claude: Repair Message JSON -> Parse + Guardrails
Queue Draft For Approval -> Advance To PENDING_APPROVAL
Classify Failure -> Record Failure
```

## wf07-approval-queue

Human approval API: review, edit, approve, reject, mark as sent.

**14 nodes.** Import from `n8n/workflows/wf07-approval-queue.json`.

| Node | Type | Talks to | Reliability | Why it is there |
| --- | --- | --- | --- | --- |
| Console API | Webhook | `POST /webhook/optiflow/approval/:action` | - | Protect with n8n header auth. The console sends the operator identity in x-operator; it is recorded on every decision. |
| Queue API | Webhook | `GET /webhook/optiflow/queue` | - | Returns the approval queue, highest priority and ICP score first. |
| Validate Request | Code | inline logic | - |  |
| Route Action | Switch |  | - |  |
| Approve | HTTP Request | db: `approve_outreach()` | retry x3 | Sets APPROVED, stores the edit as the final text, and releases the lead to READY_FOR_OUTREACH. An edited message is recorded as the "custom" variation so reporting does not credit the model. |
| Reject | HTTP Request | db: `reject_outreach()` | retry x3 | With regenerate=true the lead returns to RESEARCHED and workflow 6 writes a new draft. |
| Mark As Sent | HTTP Request | db: `mark_outreach_sent()` | retry x3 | Called after a human has actually sent the DM in Instagram. Opens the conversation, logs the outbound message and schedules the follow-up sequence. Idempotent. |
| Set Outcome | HTTP Request | db: `set_lead_outcome()` | retry x3 |  |
| Opt Out | HTTP Request | db: `opt_out()` | retry x3 | Permanent suppression across every current and future campaign. |
| Send Back For Rewrite | HTTP Request | db: `reject_outreach()` | retry x3 |  |
| Read Approval Queue | HTTP Request | db: `GET v_approval_queue` | retry x3 | Ordered by priority then ICP score. The view carries the research brief and the pain points so the reviewer can judge without opening another tab. |
| Respond OK | Respond to Webhook |  | - |  |
| Respond Queue | Respond to Webhook |  | - |  |
| Respond Bad Request | Respond to Webhook |  | - |  |

**Connections**

```
Console API -> Validate Request
Validate Request -> Route Action
Route Action (output 0) -> Approve
Route Action (output 1) -> Reject
Route Action (output 2) -> Mark As Sent
Route Action (output 3) -> Set Outcome
Route Action (output 4) -> Opt Out
Route Action (output 5) -> Send Back For Rewrite
Route Action (output 6) -> Respond Bad Request
Approve -> Respond OK
Reject -> Respond OK
Mark As Sent -> Respond OK
Set Outcome -> Respond OK
Opt Out -> Respond OK
Send Back For Rewrite -> Respond OK
Queue API -> Read Approval Queue
Read Approval Queue -> Respond Queue
```

## wf08-outreach-log

Release approved messages within the daily cap and log every send.

**13 nodes.** Import from `n8n/workflows/wf08-outreach-log.json`.

| Node | Type | Talks to | Reliability | Why it is there |
| --- | --- | --- | --- | --- |
| Release Window | Schedule Trigger | cron `0 9,11,14,16 * * 1-5` | - |  |
| Release On Demand | Webhook | `POST /webhook/optiflow/release` | - |  |
| Load Active Campaigns | HTTP Request | db: `GET campaigns` | retry x3 |  |
| Count Sent Today | HTTP Request | db: `GET outreach` | retry x3 | The daily cap is enforced against what was actually sent, not what was queued. |
| Read Send Queue | HTTP Request | db: `GET v_send_queue` | retry x3 |  |
| Apply Daily Caps + Pacing | Code | inlines `lib/followups.js` | - | Caps come from the campaign, falling back to config/followups.default.json. Overflow is held for the next window, never dropped. |
| Sending Mode | Switch |  | - | manual is the default and the only compliant path for a first-touch DM. |
| Prepare Operator Work Items | Code | inline logic | - |  |
| Notify Operator | HTTP Request | operator notification (optional) | retry x3, continues on fail | Optional: Slack/email/anything. Leave OPERATOR_WEBHOOK_URL unset to skip - the console polls the send queue anyway. |
| Send Via Instagram Graph API | HTTP Request | Instagram Graph API | retry x3, error output | Only valid INSIDE an open conversation window - i.e. for replies to someone who messaged first. A first-touch DM has no recipient_igsid and is routed to the manual path instead. |
| Conversation Already Open? | Switch |  | - | Without an open conversation the API cannot deliver a cold DM. The item falls back to a human. |
| Log Send | HTTP Request | db: `mark_outreach_sent()` | retry x3 |  |
| Respond | Respond to Webhook |  | - |  |

**Connections**

```
Release Window -> Load Active Campaigns
Release On Demand -> Load Active Campaigns
Load Active Campaigns -> Count Sent Today
Count Sent Today -> Read Send Queue
Read Send Queue -> Apply Daily Caps + Pacing
Apply Daily Caps + Pacing -> Sending Mode
Sending Mode (output 0) -> Prepare Operator Work Items
Sending Mode (output 1) -> Conversation Already Open?
Prepare Operator Work Items -> Notify Operator
Notify Operator -> Respond
Conversation Already Open? (output 0) -> Send Via Instagram Graph API
Conversation Already Open? (output 1) -> Prepare Operator Work Items
Send Via Instagram Graph API (output 0) -> Log Send
Send Via Instagram Graph API (output 1, error) -> Prepare Operator Work Items
Log Send -> Respond
```

## wf09-reply-processing

Ingest, log and classify prospect replies.

**24 nodes.** Import from `n8n/workflows/wf09-reply-processing.json`.

| Node | Type | Talks to | Reliability | Why it is there |
| --- | --- | --- | --- | --- |
| Webhook Verification | Webhook | `GET /webhook/optiflow/instagram/webhook` | - | Meta calls this once with hub.challenge when the subscription is created. |
| Answer Challenge | Code | inline logic | - |  |
| Respond Challenge | Respond to Webhook |  | - |  |
| Instagram Messages Webhook | Webhook | `POST /webhook/optiflow/instagram/webhook` | - | Official Instagram messages webhook for OUR professional account. Signed with the app secret. |
| Manual Reply Entry | Webhook | `POST /webhook/optiflow/reply/manual` | - | The operator pastes a reply they received. Identical downstream handling, so manual and API accounts behave the same. |
| Verify + Parse Inbound | Code | inline logic | - | A webhook whose signature does not verify is rejected outright. |
| Is A Real Message? | IF |  | - |  |
| Ignore | No Op |  | - |  |
| Match To Lead | HTTP Request | db: `GET v_leads_full` | retry x3 | Matched by handle, or by the lead id the console supplies. An unmatched reply is kept as an orphan rather than discarded. |
| Ensure Conversation | HTTP Request | db: `POST conversations` | retry x3, continues on fail |  |
| Log Inbound Message | HTTP Request | db: `POST conversation_messages` | retry x3 | THIS insert is what stops the sequence. The trg_conversation_messages_inbound trigger cancels every scheduled follow-up, clears next_followup_at and moves the lead to REPLIED - in the database, so it cannot be skipped. |
| Render Classification Prompt | Code | inlines `lib/prompts.js` | - |  |
| Conversation History | HTTP Request | db: `GET conversation_messages` | retry x3 |  |
| Claude: Classify Reply | HTTP Request | Claude (Messages API) | retry x3, error output | Node-level retries cover 429 and 5xx. A malformed or schema-invalid response is handled by the parser node, which sends one correction turn before failing the item. |
| Parse Classification | Code | inlines `lib/validate.js`, `lib/json.js` | - | A classifier failure degrades to "unclear + route to human". It never leaves a reply unattended. |
| Store Classification | HTTP Request | db: `PATCH conversation_messages` | retry x3 |  |
| Confirm Follow-ups Cancelled | HTTP Request | db: `cancel_followups()` | retry x3 | Belt and braces. The trigger already did this; calling it again is a no-op and makes the guarantee visible in the workflow. |
| Route By Classification | Switch |  | - | Everything else - interested, curious, question, pricing, objection, wrong person, unclear - goes to the response assistant. |
| Suppress Contact | HTTP Request | db: `opt_out()` | retry x3 |  |
| Mark Not Interested | HTTP Request | db: `set_lead_outcome()` | retry x3 |  |
| Move To Nurture | HTTP Request | db: `set_lead_outcome()` | retry x3 |  |
| Auto-reply: Do Nothing | No Op |  | - |  |
| Run Response Assistant | Execute Workflow | runs `wf10-response-assistant` | continues on fail | Drafts a suggested reply for the operator. Still nothing is sent. |
| Acknowledge | Respond to Webhook |  | - |  |

**Connections**

```
Webhook Verification -> Answer Challenge
Answer Challenge -> Respond Challenge
Instagram Messages Webhook -> Verify + Parse Inbound
Manual Reply Entry -> Verify + Parse Inbound
Verify + Parse Inbound -> Is A Real Message?
Is A Real Message? (output 0) -> Match To Lead
Is A Real Message? (output 1) -> Ignore
Match To Lead -> Ensure Conversation
Ensure Conversation -> Log Inbound Message
Log Inbound Message -> Conversation History
Conversation History -> Render Classification Prompt
Render Classification Prompt -> Claude: Classify Reply
Claude: Classify Reply -> Parse Classification
Parse Classification -> Store Classification
Store Classification -> Confirm Follow-ups Cancelled
Confirm Follow-ups Cancelled -> Route By Classification
Route By Classification (output 0) -> Suppress Contact
Route By Classification (output 1) -> Mark Not Interested
Route By Classification (output 2) -> Move To Nurture
Route By Classification (output 3) -> Auto-reply: Do Nothing
Route By Classification (output 4) -> Run Response Assistant
Suppress Contact -> Acknowledge
Mark Not Interested -> Acknowledge
Move To Nurture -> Acknowledge
Auto-reply: Do Nothing -> Acknowledge
Run Response Assistant -> Acknowledge
Ignore -> Acknowledge
```

## wf10-response-assistant

Draft a suggested reply for every inbound message, for a human to use or edit.

**15 nodes.** Import from `n8n/workflows/wf10-response-assistant.json`.

| Node | Type | Talks to | Reliability | Why it is there |
| --- | --- | --- | --- | --- |
| Called After Classification | Execute Workflow Trigger |  | - | Input: { campaign_id, batch_size?, execution_id? } |
| Sweep Open Conversations | Schedule Trigger | cron `0 */2 * * *` | - |  |
| Find Conversations Awaiting Us | HTTP Request | db: `GET v_conversations_open` | retry x3 | Catches anything the event path missed - a webhook retry, a restart mid-run. |
| Load Conversation Context | HTTP Request | db: `GET v_conversations_open` | retry x3 |  |
| Load Full Thread | HTTP Request | db: `GET conversation_messages` | retry x3 |  |
| Load Research Brief | HTTP Request | db: `GET research` | retry x3, continues on fail |  |
| Render Response Prompt | Code | inlines `lib/prompts.js` | - | company_facts is the only source of claims. Empty arrays there mean the model has no client names or metrics to quote, and the prompt tells it to write around the gap and flag it. |
| Claude: Draft Reply | HTTP Request | Claude (Messages API) | retry x3, error output | Node-level retries cover 429 and 5xx. A malformed or schema-invalid response is handled by the parser node, which sends one correction turn before failing the item. |
| Parse Suggestion | Code | inlines `lib/validate.js`, `lib/json.js` | - |  |
| Store Suggested Response | HTTP Request | db: `POST suggested_responses` | retry x3 | Status PENDING_REVIEW. The console offers Use / Edit / Regenerate / Ignore; sending is a separate, human act. |
| Needs A Person Now? | IF |  | - |  |
| Alert Operator | HTTP Request | operator notification (optional) | retry x3, continues on fail |  |
| Queued For Review | No Op |  | - |  |
| Classify Failure | Code | inlines `lib/retry.js` | - | Uses the same classifier as the test suite: 429 backs off, 5xx retries, 401/422 dead-letters immediately. |
| Record Failure | HTTP Request | db: `fail_lead()` | retry x3, continues on fail | The failed-item queue. The lead returns to its stage with an incremented retry count and an exponential backoff; the rest of the batch is unaffected. |

**Connections**

```
Called After Classification -> Load Conversation Context
Sweep Open Conversations -> Find Conversations Awaiting Us
Find Conversations Awaiting Us -> Load Conversation Context
Load Conversation Context -> Load Full Thread
Load Full Thread -> Load Research Brief
Load Research Brief -> Render Response Prompt
Render Response Prompt -> Claude: Draft Reply
Claude: Draft Reply (output 0) -> Parse Suggestion
Claude: Draft Reply (output 1, error) -> Classify Failure
Parse Suggestion -> Store Suggested Response
Store Suggested Response -> Needs A Person Now?
Needs A Person Now? (output 0) -> Alert Operator
Needs A Person Now? (output 1) -> Queued For Review
Classify Failure -> Record Failure
```

## wf11-followup-engine

Generate due follow-ups, stop the sequence on any reply, and close the loop after the final touch.

**18 nodes.** Import from `n8n/workflows/wf11-followup-engine.json`.

| Node | Type | Talks to | Reliability | Why it is there |
| --- | --- | --- | --- | --- |
| Twice Daily | Schedule Trigger | cron `0 9,14 * * 1-5` | - |  |
| Read Follow-ups Due | HTTP Request | db: `GET v_followups_due` | retry x3 | The view already excludes leads with replied_at set or a terminal status. This is the first of two guards. |
| Reply Guard | Code | inlines `lib/followups.js` | - |  |
| Still Waiting? | Switch |  | - |  |
| Cancel Sequence | HTTP Request | db: `cancel_followups()` | retry x3 |  |
| Claim Follow-up Step | HTTP Request | db: `PATCH followups` | retry x3 | The status filter makes this a claim: a parallel run PATCHing the same row gets zero rows back and skips it. |
| Load Thread + Research | HTTP Request | db: `GET outreach` | retry x3 |  |
| Load Research Brief | HTTP Request | db: `GET research` | retry x3, continues on fail |  |
| Render Follow-up Prompt | Code | inlines `lib/prompts.js`, `lib/followups.js` | - | Each step has its own intent and length limit, from config/followups.default.json. The prompt receives the real thread, including any human edit, because that is what the prospect actually read. |
| Claude: Write Follow-up | HTTP Request | Claude (Messages API) | retry x3, error output | Node-level retries cover 429 and 5xx. A malformed or schema-invalid response is handled by the parser node, which sends one correction turn before failing the item. |
| Parse + Check Follow-up | Code | inlines `lib/validate.js`, `lib/json.js` | - | A follow-up that reduces to "just following up" is flagged before a human ever sees it. |
| Queue Follow-up For Approval | HTTP Request | db: `POST outreach` | retry x3 | Follow-ups go through the same human approval queue as first messages - campaign setting followups.require_human_approval. |
| Link Draft To Step | HTTP Request | db: `PATCH followups` | retry x3 |  |
| Was That The Final Touch? | IF |  | - |  |
| Move To Nurture | HTTP Request | db: `set_lead_outcome()` | retry x3 | After the final touch the lead moves to NURTURE for a later recheck - it is not deleted and not messaged again by this sequence. |
| Done | No Op |  | - |  |
| Classify Failure | Code | inlines `lib/retry.js` | - | Uses the same classifier as the test suite: 429 backs off, 5xx retries, 401/422 dead-letters immediately. |
| Record Failure | HTTP Request | db: `fail_lead()` | retry x3, continues on fail | The failed-item queue. The lead returns to its stage with an incremented retry count and an exponential backoff; the rest of the batch is unaffected. |

**Connections**

```
Twice Daily -> Read Follow-ups Due
Read Follow-ups Due -> Reply Guard
Reply Guard -> Still Waiting?
Still Waiting? (output 0) -> Claim Follow-up Step
Still Waiting? (output 1) -> Cancel Sequence
Claim Follow-up Step -> Load Thread + Research
Load Thread + Research -> Load Research Brief
Load Research Brief -> Render Follow-up Prompt
Render Follow-up Prompt -> Claude: Write Follow-up
Claude: Write Follow-up (output 0) -> Parse + Check Follow-up
Claude: Write Follow-up (output 1, error) -> Classify Failure
Parse + Check Follow-up -> Queue Follow-up For Approval
Queue Follow-up For Approval -> Link Draft To Step
Link Draft To Step -> Was That The Final Touch?
Was That The Final Touch? (output 0) -> Move To Nurture
Was That The Final Touch? (output 1) -> Done
Classify Failure -> Record Failure
```

## wf12-analytics

Daily metrics, narrative report and the optional Google Sheets mirror.

**15 nodes.** Import from `n8n/workflows/wf12-analytics.json`.

| Node | Type | Talks to | Reliability | Why it is there |
| --- | --- | --- | --- | --- |
| Daily Report | Schedule Trigger | cron `0 18 * * *` | - |  |
| Report On Demand | Webhook | `GET /webhook/optiflow/report` | - |  |
| Load Active Campaigns | HTTP Request | db: `GET campaigns` | retry x3 |  |
| Compute Daily Metrics | HTTP Request | db: `daily_report()` | retry x3 | One SQL function returns every counter plus the niche/state/angle breakdowns. The model never recalculates them. |
| Prepare Report Input | Code | inlines `lib/prompts.js` | - | ranking_metric stays null unless the operator picks one, which is what stops the report inventing a "best niche" from three replies. |
| Claude: Write Narrative | HTTP Request | Claude (Messages API) | retry x3, error output | Node-level retries cover 429 and 5xx. A malformed or schema-invalid response is handled by the parser node, which sends one correction turn before failing the item. |
| Parse Narrative | Code | inlines `lib/validate.js`, `lib/json.js` | - |  |
| Store Daily Metrics | HTTP Request | db: `POST daily_metrics` | retry x3, continues on fail |  |
| Format Report | Code | inline logic | - |  |
| Deliver Report | HTTP Request | operator notification (optional) | retry x3, continues on fail |  |
| Respond | Respond to Webhook |  | - |  |
| Sheets Mirror Enabled? | IF |  | - | Off by default. The database is the source of truth; the sheet is a read-only convenience view. |
| Read Sheets Export View | HTTP Request | db: `GET v_sheets_export` | retry x3 | The view defines the exact column set, so the sheet layout lives in SQL rather than in a node. |
| Push To Google Sheets | HTTP Request | Google Sheets | retry x3, error output | Replace with the n8n Google Sheets node if you prefer its OAuth handling - config/providers.json capability "mirror". |
| Mirror Disabled | No Op |  | - |  |

**Connections**

```
Daily Report -> Load Active Campaigns
Report On Demand -> Load Active Campaigns
Load Active Campaigns -> Compute Daily Metrics
Compute Daily Metrics -> Prepare Report Input
Prepare Report Input -> Claude: Write Narrative
Claude: Write Narrative -> Parse Narrative
Parse Narrative -> Store Daily Metrics
Store Daily Metrics -> Format Report
Store Daily Metrics -> Sheets Mirror Enabled?
Format Report -> Deliver Report
Deliver Report -> Respond
Sheets Mirror Enabled? (output 0) -> Read Sheets Export View
Sheets Mirror Enabled? (output 1) -> Mirror Disabled
Read Sheets Export View -> Push To Google Sheets
```

## wf13-orchestrator

Campaign settings -> discovery -> cleaning -> enrichment -> qualification -> research -> outreach generation -> approval -> release -> follow-ups.

**18 nodes.** Import from `n8n/workflows/wf13-orchestrator.json`.

| Node | Type | Talks to | Reliability | Why it is there |
| --- | --- | --- | --- | --- |
| Pipeline Tick | Schedule Trigger | cron `*/15 6-20 * * 1-5` | - |  |
| Run Now | Webhook | `POST /webhook/optiflow/run` | - | Body: { campaign_id?, stages?: ["discovery","clean",...] }. Omit both to run every active campaign end to end. |
| Load Active Campaigns | HTTP Request | db: `GET campaigns` | retry x3 |  |
| Plan This Tick | Code | inlines `lib/stages.js` | - | Stage selection is data, not wiring: pass { stages: ["qualify"] } to the webhook to re-run one stage for one campaign. |
| Any Active Campaign? | IF |  | - |  |
| Nothing To Run | No Op |  | - |  |
| For Each Campaign | Loop Over Items |  | - |  |
| 1. Discovery | Execute Workflow | runs `wf01-lead-discovery` | continues on fail | Runs "wf01-lead-discovery". continueOnFail is deliberate: one failing stage must not stop the campaign. |
| 2. Clean + Dedupe | Execute Workflow | runs `wf02-clean-dedupe` | continues on fail | Runs "wf02-clean-dedupe". continueOnFail is deliberate: one failing stage must not stop the campaign. |
| 3. Enrichment | Execute Workflow | runs `wf03-enrichment` | continues on fail | Runs "wf03-enrichment". continueOnFail is deliberate: one failing stage must not stop the campaign. |
| 4. Qualification | Execute Workflow | runs `wf04-qualification` | continues on fail | Runs "wf04-qualification". continueOnFail is deliberate: one failing stage must not stop the campaign. |
| 5. Research | Execute Workflow | runs `wf05-research` | continues on fail | Runs "wf05-research". continueOnFail is deliberate: one failing stage must not stop the campaign. |
| 6. Outreach Generation | Execute Workflow | runs `wf06-outreach-generation` | continues on fail | Runs "wf06-outreach-generation". continueOnFail is deliberate: one failing stage must not stop the campaign. |
| 8. Release Approved | Execute Workflow | runs `wf08-outreach-log` | continues on fail | Runs "wf08-outreach-log". continueOnFail is deliberate: one failing stage must not stop the campaign. |
| 11. Follow-ups | Execute Workflow | runs `wf11-followup-engine` | continues on fail | Runs "wf11-followup-engine". continueOnFail is deliberate: one failing stage must not stop the campaign. |
| Record Tick Outcome | Code | inline logic | - |  |
| Store Run Record | HTTP Request | db: `POST workflow_runs` | retry x3, continues on fail | Resume evidence: which stages ran, which failed, and where the tick stopped. |
| Respond | Respond to Webhook |  | - |  |

**Connections**

```
Pipeline Tick -> Load Active Campaigns
Run Now -> Load Active Campaigns
Load Active Campaigns -> Plan This Tick
Plan This Tick -> Any Active Campaign?
Any Active Campaign? (output 0) -> For Each Campaign
Any Active Campaign? (output 1) -> Nothing To Run
For Each Campaign (output 0) -> Record Tick Outcome
For Each Campaign (output 1) -> 1. Discovery
1. Discovery -> 2. Clean + Dedupe
2. Clean + Dedupe -> 3. Enrichment
3. Enrichment -> 4. Qualification
4. Qualification -> 5. Research
5. Research -> 6. Outreach Generation
6. Outreach Generation -> 8. Release Approved
8. Release Approved -> 11. Follow-ups
11. Follow-ups -> For Each Campaign
Record Tick Outcome -> Store Run Record
Store Run Record -> Respond
```
